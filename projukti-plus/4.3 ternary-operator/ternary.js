/**
 * Ternary Operator
 * ( condition ) ? true : false;
 * 
 * marks >= 40 ? 'Passed' : 'Failed'
 */

let marks = 40;
const result   = marks >= 40 ? 'You are passed' : 'You are failed';
console.log( result );